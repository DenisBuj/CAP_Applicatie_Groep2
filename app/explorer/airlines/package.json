{
  "name": "primepath.explorer.airlines",
  "version": "0.0.1",
  "description": "PrimePath Explorer — Inzichten (HR/Admin)",
  "main": "webapp/index.html",
  "dependencies": {},
  "devDependencies": {
    "@ui5/cli": "^4.0.33",
    "@sap/ux-ui5-tooling": "1",
    "@sap-ux/eslint-plugin-fiori-tools": "^9.0.0",
    "eslint": "^9"
  },
  "scripts": {
    "start": "ui5 serve",
    "build:cf": "rm -rf dist && mkdir -p dist && cd webapp && zip -rqX ../dist/primepath.explorer.airlines.zip . --exclude '*.zip'",
    "deploy-config": "npx -p @sap/ux-ui5-tooling fiori add deploy-config cf"
  }
}
